export default function Home() {
  return (
    <main
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100vh",
        fontFamily: "Inter, system-ui, sans-serif",
        color: "#1A1D23",
        backgroundColor: "#FFFFFF",
      }}
    >
      <h1>AnExcel — Setup OK</h1>
    </main>
  );
}
